{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "leaderboard": {
      ".read": true,
      "players": {
        "$uid": {
          ".write": "auth != null && auth.uid === $uid"
        }
      },
      "ghosts": {
        "$uid": {
          ".write": "auth != null && auth.uid === $uid"
        }
      },
      "community_benchmarks": {
        ".write": "auth != null"
      }
    }
  }
}
